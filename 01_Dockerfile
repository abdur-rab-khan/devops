FROM ubuntu


RUN echo "Setting started" && \
    apt-get update -y && \
    apt-get upgrade -y && \
    echo "Installing python3" && \
    apt-get install python3 -y


RUN --mount=type=secret,id=secret_value cat /run/secrets/secret_value && sleep 10 && echo "DONE........"